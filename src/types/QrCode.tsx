export type Phone = {
  type: string;
  value: string;
};
export type Address = {
  type: string;
  label: string;
  street: string;
  city: string;
  region: string;
  postalCode: string;
  country: string;
};
